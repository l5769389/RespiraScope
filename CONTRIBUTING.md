# 贡献指南

感谢你关注 RespiraScope。这个项目主要面向呼吸信号采集、滤波、可视化和设备联调场景，欢迎提交 bug 修复、文档改进、算法验证样例和前端体验优化。

## 本地开发

```bash
uv sync
uv run pytest
uv run python scripts/dev.py
```

Windows 下也可以使用：

```powershell
.\scripts\dev.ps1
```

## 提交建议

- 改动尽量保持小而清晰。
- 修改滤波、峰谷识别、记录文件结构或接口 schema 时，请同步更新测试和文档。
- 不要提交真实患者数据、真实设备地址、生产配置、日志、token 或客户/医院信息。
- 示例数据应使用模拟数据或完全脱敏的数据。
- 前端改动请确认 Monitor、模拟信号设置、使用指南和接口文档页面都能正常打开。

## 测试重点

涉及信号处理时，建议至少考虑这些场景：

- 稳定呼吸。
- 浅呼吸。
- 屏气或长时间无明显波动。
- 体动噪声。
- 传感器突然断流和恢复。
- 记录前后冗余数据是否正确标记为 `pre`、`record`、`post`。

## 医疗边界

RespiraScope 用于工程验证、算法学习和设备调试，不作为医疗诊断、治疗决策或临床监护依据。任何临床场景使用都需要经过额外的验证、合规评估和风险控制。
